import 'package:flutter/material.dart';

class OfferingDetail extends StatelessWidget {
  const OfferingDetail({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Offering Details'),
      ),
      body: Center(
        child: Placeholder(),
      ),
    );
  }
}