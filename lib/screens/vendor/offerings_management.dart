import 'package:flutter/material.dart';

class OfferingsManagement extends StatelessWidget {
  const OfferingsManagement({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Offering Management'),
      ),
      body: const Center(
        child: Placeholder(),
      ),
    );
  }
}